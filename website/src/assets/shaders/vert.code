float elevation = noise3D(v2f_pos, u_seed, u_elevScale, u_elevLac, u_elevPersistence, u_elevOctaves);
float clamped = clamp(elevation, 0.5, 1.0);
float value = smoothstep(0., 1., clamped);
float jitter = value * value * .05;
vec3 transformed = position + normal * jitter;
v2f_pos = transformed;